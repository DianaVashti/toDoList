extends layout


block content
  h1 Select a ToDo list
    for table in tables
      a(href=`/table/${table.id}`)
        h1 #{table.table_name}
    include ./add_new_table.pug

    a.btn.btn-primary(href='#', type=button, data-toggle='modal', data-target='#newList') Create New List
