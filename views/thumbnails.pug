extends layout


block content
  a.btn.btn-primary(href='#', type=button, data-toggle='modal', data-target='#newList') modalAppear
  if tables.length > 0
    for table in tables
      a(href=`/table/${table.id}`)
        h1 #{table.table_name}
  include ./add_new_table.pug
