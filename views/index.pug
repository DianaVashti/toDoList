extends layout

block content
  include ./form.pug
  h1 ToDO
  for todo in allToDos
    div  #{todo.id} #{todo.name} - #{todo.description}
      a(href=`/delete/${todo.id}`) delete
  a(href='#', type=button, data-toggle='modal', data-target='.modal') modalAppear
